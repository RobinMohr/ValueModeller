{
  "name": "tecfactory",
  "version": "1.0.0",
  "description": "TecFactory — WebSocket-based web UI to monitor and control Kiro ACP agents",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "vitest": "^3.2.1",
    "@vitest/coverage-v8": "^3.2.1",
    "supertest": "^7.1.0"
  }
}

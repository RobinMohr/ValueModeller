/**
 * Azure Functions v4 — Task API
 *
 * This is the entry point for the Azure Functions app.
 * Function definitions in src/functions/ are auto-discovered
 * by the Azure Functions runtime.
 */

import { app } from '@azure/functions';

app.setup({});
